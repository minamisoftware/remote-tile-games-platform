var socket;

$(document).ready(function(){

//    window.socket = io.connect( document.location.origin );
//    if (window.socket) {
//        window.socket.emit('hello', { id: 123 });
//    }

});
